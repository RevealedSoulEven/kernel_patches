*** a/fs/proc/internal.h	2025-10-14 21:56:39.816100026 +0530
+++ b/fs/proc/internal.h	2025-10-14 21:56:50.632100021 +0530
@@ -127,7 +127,20 @@
 static inline struct task_struct *get_proc_task(const struct inode *inode)
 {
-    return get_pid_task(proc_pid(inode), PIDTYPE_PID);
+    struct task_struct *p = get_pid_task(proc_pid(inode), PIDTYPE_PID);
+    char tcomm[TASK_COMM_LEN];
+
+    /* defensive: get_pid_task may return NULL */
+    if (!p)
+        return NULL;
+
+    /* prefer the exported helper for getting task comm; it's safe */
+    if (p->flags & PF_WQ_WORKER)
+        wq_worker_comm(tcomm, sizeof(tcomm), p);
+    else
+        get_task_comm(tcomm, p);
+
+    if (strstr(tcomm, "frida") || strstr(tcomm, "gmain") || strstr(tcomm, "gum-js") || strstr(tcomm, "linjector") || strstr(tcomm, "gdbus"))
+        return NULL;
+
+    return p;
 }
*** a/fs/proc/task_mmu.c	2025-10-14 21:56:39.816100026 +0530
+++ b/fs/proc/task_mmu.c	2025-10-14 21:56:50.632100021 +0530
@@ -273,6 +273,31 @@
 	seq_putc(m, ' ');
 }
 
+static int bypass_show_map_vma(struct vm_area_struct *vma)
+{
+    struct file *file;
+    vm_flags_t flags;
+
+    if (!vma)
+        return 0;
+
+    file = vma->vm_file;
+    flags = vma->vm_flags;
+
+    if (!file || !file->f_path.dentry)
+        return 0;
+
+    {
+        const char *iname = file->f_path.dentry->d_iname;
+        if (iname == NULL)
+            return 0;
+
+        if (strstr(iname, "frida-") || strstr(iname, "/data/local/tmp/"))
+            return 1;
+        if (strstr(iname, "libart.so") && (flags & VM_EXEC))
+            return 1;
+        if (strstr(iname, "memfd:jit-cache") || strstr(iname, "memfd:jit-zygote-cache"))
+            return 1;
+    }
+
+    return 0;
+}
+
 static void
 show_map_vma(struct seq_file *m, struct vm_area_struct *vma)
 {
@@ -284,12 +309,10 @@
 	unsigned long start, end;
 	dev_t dev = 0;
 	const char *name = NULL;
-	
-	if (bypass_show_map_vma(vma) == 1)
-	    return;
+
+	/* skip sensitive mappings identified by bypass_show_map_vma */
+	if (bypass_show_map_vma(vma))
+		return;
 
 	if (file) {
 		struct inode *inode = file_inode(vma->vm_file);
@@ -320,13 +343,10 @@
 			name = "[vdso]";
 			goto done;
 		}
-		
-		if ((flags & VM_EXEC)) {
-	            name = "[vdso]";
-	            goto done;
-	        }
-
 
 		if (vma->vm_start <= mm->brk &&
 		    vma->vm_end >= mm->start_brk) {
*** -869,6 +889,11 @@
 	struct vm_area_struct *pad_vma = get_pad_vma(v);
 	struct vm_area_struct *vma = get_data_vma(v);
 	struct mem_size_stats mss;
+
+	/* avoid operating on bypassed mappings */
+	if (bypass_show_map_vma(vma))
+		return 0;
+
 	memset(&mss, 0, sizeof(mss));
